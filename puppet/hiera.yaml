---
version: 5
defaults:
  # The default value for "datadir" is "data" under the same directory as the hiera.yaml
  # file (this file)
  # When specifying a datadir, make sure the directory exists.
  # See https://puppet.com/docs/puppet/latest/environments_about.html for further details on environments.
  # datadir: data
  # data_hash: yaml_data
hierarchy:
#  - name: "Hiera-HTTP lookup"
#    lookup_key: hiera_http
#    uris:
#      - http://localhost:5984/host/%{trusted.certname}
#      - http://localhost:5984/dc/%{facts.location}
#      - http://localhost:5984/role/%{facts.role}
#    options:
#      output: json
#      ignore_404: true
  - name: "SWEAGLE HIERA"
    #lookup_key: Sweagle_data_lookup # sweagle data_lookup backend
    data_hash: sweagle_data_hash  # sweagle data_hash backend
    options:
      #sweagle_cds: "<YOUR_CDS>"
      sweagle_node: "<YOUR NODE>"
      #sweagle_tenant: "<YOUR_TENANT>"
      #sweagle_tenant: "https://testing.sweagle.com"
      #sweagle_token: "<YOUR_TOKEN>"
